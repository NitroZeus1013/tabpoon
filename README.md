# A simple extension for customizing switching between open browser tab.

Currently it only supports:

- go forward
- go backward
- go to last used tab(meaning you can keep switching between 2 tabs quickly)
